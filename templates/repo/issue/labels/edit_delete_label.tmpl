<div class="ui small basic delete modal">
	<div class="ui icon header">
		{{svg "octicon-trashcan"}}
		{{.i18n.Tr "repo.issues.label_deletion"}}
	</div>
	<div class="content">
		<p>{{.i18n.Tr "repo.issues.label_deletion_desc"}}</p>
	</div>
	<div class="actions">
		<div class="ui red basic inverted cancel button">
			<i class="remove icon"></i>
			{{.i18n.Tr "modal.no"}}
		</div>
		<div class="ui green basic inverted ok button">
			<i class="checkmark icon"></i>
			{{.i18n.Tr "modal.yes"}}
		</div>
	</div>
</div>

<div class="ui small edit-label modal">
	<div class="header">
		{{.i18n.Tr "repo.issues.label_modify"}}
	</div>
	<div class="content">
		<form class="ui edit-label form inline" action="{{$.Link}}/edit" method="post">
			{{.CsrfTokenHtml}}
			<input id="label-modal-id" name="id" type="hidden">
			<div class="required field-set">
				<label for="edit_title" class="required">{{.i18n.Tr "repo.issues.label_title"}}</label>
				<input id="edit_title" name="title" class="field new-label-input emoji-input" placeholder="{{.i18n.Tr "repo.issues.new_label_placeholder"}}" autofocus required maxlength="50">
			</div>
			<div class="inline field-set">
				<label for="edit_desc">{{.i18n.Tr "repo.issues.label_description"}}</label>
				<input id="edit_desc" name="description" class="field new-label-desc-input" placeholder="{{.i18n.Tr "repo.issues.new_label_desc_placeholder"}}" maxlength="200">
			</div>
			<div class="inline field-set">
				<label for="edit_priority">{{.i18n.Tr "repo.issues.label_priority"}}</label>
				<select id="edit_priority" class="ui field dropdown selection new-label-priority-input" name="priority">
					<option value=""></option>
					{{range $priority := .LabelPriorities}}
					<option value="{{$priority}}">{{$.i18n.Tr (printf "repo.issues.label_priority_%s" $priority)}}</option>
					{{end}}
				</select>
			</div>
			<div class="inline field-set">
				<label for="edit_color" class="required">{{.i18n.Tr "repo.issues.label_color"}}</label>
				<div class="field">
					<div class="color picker">
						<input id="edit_color" class="color-picker" name="color" value="#70c24a" required maxlength="7">
					</div>
					<div class="precolors">
						{{template "repo/issue/label_precolors"}}
					</div>
				</div>
			</div>
		</form>
	</div>
	<div class="actions">
		<div class="ui negative button">
			{{.i18n.Tr "cancel"}}
		</div>
		<div class="ui positive button">
			{{.i18n.Tr "save"}}
		</div>
	</div>
</div>

